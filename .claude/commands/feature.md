Add the following feature idea to `Features/Feature Ideas.md`.

Add it to the most appropriate category (Security, Quarantine, User Features, Admin Features, SpamAssassin, Delivery, Email Modification, Marketing/Onboarding, etc.). Use the `- [ ]` checkbox format.

If the idea needs its own detailed spec file, ask if I want you to create a new file in `Features/`.

Feature idea: $ARGUMENTS
